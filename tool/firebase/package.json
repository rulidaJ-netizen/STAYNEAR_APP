{
  "name": "staynear-firebase-validation",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --project demo-staynear --config ../../firebase.json --only auth,firestore \"node --test rules.test.cjs\"",
    "start": "firebase emulators:start --project demo-staynear --config ../../firebase.json --only auth,firestore,functions --import ../../.firebase/emulator-data --export-on-exit ../../.firebase/emulator-data"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
